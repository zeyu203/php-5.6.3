// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("cutstring", "for cutstring support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("cutstring", "enable cutstring support", "no");

if (PHP_CUTSTRING != "no") {
	EXTENSION("cutstring", "cutstring.c");
}

